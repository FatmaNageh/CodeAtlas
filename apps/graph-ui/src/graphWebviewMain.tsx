import React from "react";
import ReactDOM from "react-dom/client";
import GraphWebviewApp from "./GraphWebviewApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GraphWebviewApp />
  </React.StrictMode>
);
